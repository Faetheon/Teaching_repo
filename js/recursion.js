/**
  *  Rules
  *     1. You can only call the function itself
  *     2. You can't return a hardcoded value e.g. return 7 or return 'asdf';
  *     3. All tests must pass
  *     4. Only use MDN for googling
  *     5. No passing addtional arguments
 */

function exponent(base, power) {
  // Write code here
}

function isPalindrome(string) {
  // Return true if the string is a palindrome false if it isn't
}